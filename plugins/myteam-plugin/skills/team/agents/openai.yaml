interface:
  display_name: "MyTeam"
  short_description: "Runs PM analysis first and selects only the necessary specialist roles."
  default_prompt: "$team Analyze this requirement from the MyTeam perspective. Run PM analysis first and select only the necessary specialist roles."
