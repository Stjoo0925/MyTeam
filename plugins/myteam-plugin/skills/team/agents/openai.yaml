interface:
  display_name: "MyTeam"
  short_description: "Routes first, minimizes agents, and executes actionable work through contracts, tools, artifacts, checkpoints, or automation when needed."
  default_prompt: "$team Route this request, choose Light, Standard, or Deep mode, classify execution pattern and surface, use Contract Officer assignment and validation when needed, execute only required agents, and provide the final team decision."
